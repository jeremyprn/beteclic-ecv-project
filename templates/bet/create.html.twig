{% extends 'base.html.twig' %}

{% block title %}Parier{% endblock %}

{% block body %}
    <div class="form_layout">
        <div class="form">
            <div class="form_title">
                <h1 class="add-title">{{ event.title }}</h1>
                <h2 class="sub-title">{{ selectionEvent.label }}</h2>
            </div>
            {{ include('bet/_create-form.html.twig') }}
        </div>
    </div>
{% endblock %}
