<form method="post" action="{{ path('app_selection_event_delete', {'id': selection_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ selection_event.id) }}">
    <button class="btn">Delete</button>
</form>
