{% extends 'base.html.twig' %}

{% block title %}SelectionEvent{% endblock %}

{% block body %}
    <h1>SelectionEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ selection_event.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ selection_event.label }}</td>
            </tr>
            <tr>
                <th>Odd</th>
                <td>{{ selection_event.odd }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_selection_event_index') }}">back to list</a>

    <a href="{{ path('app_selection_event_edit', {'id': selection_event.id}) }}">edit</a>

    {{ include('selection_event/_delete_form.html.twig') }}
{% endblock %}
